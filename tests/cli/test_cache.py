def test_cache_cli_stats(tmp_path):
    # TODO(learner): Construct CliRunner, mock Settings.cache_dir → tmp_path,
    #   populate db, invoke `codeograph cache stats`, assert output shape.
    pass


def test_cache_cli_purge_dry_run(tmp_path):
    # TODO(learner): Assert purge without --force keeps all entries; with --force removes them.
    pass
