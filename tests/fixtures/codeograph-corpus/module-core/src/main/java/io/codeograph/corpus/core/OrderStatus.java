package io.codeograph.corpus.core;

/**
 * Lifecycle states of an Order.
 * Exercises: enum node, four constants, no methods.
 */
public enum OrderStatus {
    PENDING,
    CONFIRMED,
    SHIPPED,
    CANCELLED
}
