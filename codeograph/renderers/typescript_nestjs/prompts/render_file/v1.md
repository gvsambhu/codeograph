---
id: render_file
version: v1
purpose: RENDER
required_vars: [fqcn, class_json, annotation_json, db_layer, unsupported_feature_policy, webflux_policy]
optional_vars: [security_hint, webflux_hint]
cacheable_segments: [system]
content_hash_pin: TODO
---
# System
<!-- TODO(learner): Write the system prompt.
     This is the primary learner-write zone for DC3.

     Guidance questions to answer here:
       1. What role/persona should the model adopt?
          (e.g. "senior TypeScript engineer familiar with NestJS 10 and TypeORM")
       2. What output format should it produce?
          (raw TypeScript source only, or fenced code block, or JSON envelope?)
       3. What invariants must it always respect?
          (decorators to use, class naming conventions, import paths, stub shape)
       4. How should it handle the feature-policy hints injected via optional_vars?

     Look at annotate_node/v1.md §System for the style and level of detail expected.
-->

# User
<!-- TODO(learner): Write the user prompt.
     This is where the per-class context is injected.

     Context you have available (injected by TypeScriptRenderer._call_llm):
       - <<fqcn>>             — fully-qualified Java class name
       - <<class_json>>       — ClassNode serialised as JSON (graph node)
       - <<annotation_json>>  — LLM annotation for this class (Pass 1 output)
       - <<db_layer>>         — "typeorm" (v1 only)
       - <<unsupported_feature_policy>> — "stub_with_todo" | "refuse_to_render" | "best_effort"
       - <<webflux_policy>>   — "stub_with_todo" | "refuse_to_render"
       - <<security_hint>>    — (optional) injected when Spring Security annotations found
       - <<webflux_hint>>     — (optional) injected when Mono<T>/Flux<T> types found

     Design considerations:
       - How much of class_json should you surface verbatim vs summarise?
       - Should annotation_json's "conversion_complexity" field influence the instructions?
       - What explicit output constraints will produce TypeScript that tsc accepts?
       - How do you phrase the stub-with-todo instruction so the model emits
         a useful TODO comment that guides the human implementer?

     Reference ADR-010 §3 (the Spring → NestJS idiom mapping table) as you write.
     Every mapping decision in the ADR should be either reflected in the prompt
     or delegated to the model with an explicit instruction.
-->
