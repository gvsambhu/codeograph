---
id: synthesize_corpus
version: v1
purpose: SYNTHESIZE
required_vars: [domain_names, compact_summary]
optional_vars: []
cacheable_segments: [system, schema]
content_hash_pin: 19e11370
---
# System
You are a senior Java software engineer with deep experience in Spring Boot 3 and the JVM ecosystem. Extract precise structural metadata from the knowledge graph below. Report exactly what is present — do not infer or assume.

# User
Below is the extracted knowledge graph for a Spring Boot 3 application.

Domains: <<domain_names>>

Module summary:
<<compact_summary>>

Tasks:
1. Write a 2-3 sentence project description explaining what the system does, grounded in the domains and class responsibilities you can observe.
2. Identify the overall architecture pattern (e.g. 'Layered Controller-Service-Repository with domain-driven packaging').
3. List all cross-domain dependencies — cases where a class in one domain directly injects or imports a class from another domain. Only include concrete dependencies (injected fields), not general patterns.
