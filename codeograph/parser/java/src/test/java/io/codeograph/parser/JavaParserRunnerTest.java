package io.codeograph.parser;

/**
 * Tests have moved to dedicated test classes:
 *   ParsedFileAssemblerTest  — structural extraction + field/method metadata
 *   ComplexityCalculatorTest — cyclomatic, cognitive, LOC, WMC, CBO
 *   Lcom4CalculatorTest      — LCOM4 connected-components
 *   LombokSynthesizerTest    — Lombok synthesis (Issue #2)
 */
class JavaParserRunnerTest {}
