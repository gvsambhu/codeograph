---
id: synthesize_corpus
version: v1
purpose: SYNTHESIZE
required_vars: [domain_names, compact_summary]
optional_vars: []
cacheable_segments: [system, schema]
content_hash_pin: da731619
---
# System
You are a senior Java software engineer specialising in Spring Boot 3, JPA, QueryDSL, and Spring HATEOAS. Extract precise structural metadata from Java source code. Report exactly what is written in the code — do not infer or assume.

# User
Below is the extracted knowledge graph for a Spring Boot 3 REST API managing a DVD rental store ([redacted] database).

Domains: <<domain_names>>

Module summary:
<<compact_summary>>

# TODO(learner): refine wording for SynthesisResult
Tasks:
1. Write a 2-3 sentence project description explaining what the system does.
2. Identify the overall architecture pattern (e.g. 'Layered Controller-Service-Repository with domain-driven packaging').
3. List all cross-domain dependencies — cases where a class in one domain directly injects or imports a class from another domain. Only include concrete dependencies (injected fields), not general patterns.
